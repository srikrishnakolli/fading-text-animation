name: fading_text_animation
description: A new Flutter project.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.0.3  # Ensure proper indentation

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
